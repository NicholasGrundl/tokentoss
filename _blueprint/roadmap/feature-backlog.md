# Feature Backlog

Plans, fixes, and enhancements not currently scheduled to a phase. Each entry should be one-to-three lines. When an item commits to a phase, lift it to a `[planning]<name>-v1.md` spec in `features/` and link it from `ROADMAP.md`.

